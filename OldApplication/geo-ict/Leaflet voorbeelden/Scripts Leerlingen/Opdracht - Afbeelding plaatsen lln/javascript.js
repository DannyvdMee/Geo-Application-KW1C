
$(document).ready(function () 
 { 
     // Aanmaken van de map
     
     // Event dat wordt uitgevoerd na het aanpassen textboxwaarde met id afbeelding
     
     { 
         EXIF.getData(e.target.files[0], function () 
         { 
             // Ophalen lengtegraad van de foto
             
             // Ophalen breedtegraad van de foto 
 
             // Omzetten coordinaat type van de foto (Denk aan de standaardfunctie .split)
             // Lengtegraad:
             // Breedtegraad:

             // Plaatsen van de marker op de coordinaten waarop de foto gemaakt is
             
             // Laat de kaart centreren op de marker
             
             // Verander de tekst in de div met id coord zodat het coordinaat hier getoond wordt
             
         }); 
     } 
 }); 